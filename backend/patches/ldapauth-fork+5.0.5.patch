diff --git a/node_modules/ldapauth-fork/lib/ldapauth.js b/node_modules/ldapauth-fork/lib/ldapauth.js
index 88398ed..6bef337 100644
--- a/node_modules/ldapauth-fork/lib/ldapauth.js
+++ b/node_modules/ldapauth-fork/lib/ldapauth.js
@@ -279,7 +279,28 @@ LdapAuth.prototype._search = function (searchBase, options, callback) {
 
       var items = [];
       searchResult.on('searchEntry', function (entry) {
-        items.push(entry.object);
+        var ldapUser = {};
+        var attributes = entry.pojo && entry.pojo.attributes;
+
+        if (attributes) {
+          attributes.forEach(function (attribute) {
+            ldapUser[attribute.type] = attribute.type === 'memberOf'
+              ? attribute.values
+              : attribute.values[0];
+          });
+        } else if (entry.object) {
+          Object.keys(entry.object).forEach(function (key) {
+            var value = entry.object[key];
+            ldapUser[key] = key === 'memberOf' || !Array.isArray(value)
+              ? value
+              : value[0];
+          });
+        }
+
+        ldapUser.dn = (entry.pojo && entry.pojo.objectName) ||
+          entry.objectName ||
+          (entry.object && entry.object.dn);
+        items.push(ldapUser);
         if (self.opts.includeRaw === true) {
           items[items.length - 1]._raw = entry.raw;
         }
